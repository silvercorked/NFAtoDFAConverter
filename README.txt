java version "14.0.1" 2020-04-14
Java(TM) SE Runtime Environment (build 14.0.1+7)
Java HotSpot(TM) 64-Bit Server VM (build 14.0.1+7, mixed mode, sharing)

Steps:
1 - extract
2 - run command "javac *.java"
3 - run command "java NFAConvert {file}"

The program will output some info in the console
to inform you how it is progressing. It will also let
you know what the output file it creates is called.
the output file should be deposited in the same folder
as the program.